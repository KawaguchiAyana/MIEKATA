<%= stylesheet_link_tag 'show', :media => "all" %>
<h2>投稿詳細</h2>
<div class="tweets-container">

  <div class="tweet">

  <p class="about"><%= @tweet.about %></p><br>
  <p>Date：<%= @tweet.Date.strftime("%Y年%m月%d日 %H:%M開演") %></p>
  <p>Seat：<%= @tweet.seat %></p>
  <p>Price：<%= @tweet.price %></p>
  <p class="picture"><%= image_tag @tweet.image_url, size: "300x240" if @tweet.image? %></p><br>
  <p>Comment</p>
  <div class="comment"><%= @tweet.comment %></div><br>

    <h4>評価：
    <span class="star-rating">
       <span class="star-rating-front" style="width: <%= getPercent(@tweet.overall)%>%;">★★★★★</span>
       <span class="star-rating-back">★★★★★</span>
    </span> 
    </h4>
    </p>

  <br><div class="post-date"><%= @tweet.created_at.strftime("%Y/%m/%d %H:%M") %></div>
  


<% if user_signed_in? && current_user.id == @tweet.user_id %>
  <%= link_to "編集する", edit_tweet_path(@tweet.id) %>
  <div class="deletes">
    <%= button_to "削除する", tweet_path(@tweet.id), method: :delete,class:"delete" %>
  </div>
<% end %>

<% @tweet.tags.each do |tag| %>
    <span><%= tag.name %></span>
<% end %>




</div>
</div>
<%= link_to "投稿一覧に戻る", tweets_path %>

